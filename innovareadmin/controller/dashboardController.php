<?php

// -- GET COURSE COUNT -- //

	// -- total -- //
		$total_course_count = count( $innovare->getCourseCount() ) ;

		// echo $course_count;die();

	// -- active -- //
		$active_course_count = count( $innovare->getActiveCourseCount() ) ;

		// echo $course_count;die();

	// -- archived -- //
		$archived_course_count = count( $innovare->getArchivedCourseCount() ) ;

		// echo $course_count;die();

	// -- cat -- //
		$course_cat_count = count( $innovare->getCourseCatCount() ) ;

		// echo $course_count;die();


// -- GET EVENT COUNT -- //

	// -- total -- //
		$total_event_count = count( $innovare->getEventCount() ) ;

		// echo $course_count;die();

	// -- active -- //
		$active_event_count = count( $innovare->getActiveEventCount() ) ;

		// echo $course_count;die();

	// -- archived -- //
		$archived_event_count = count( $innovare->getArchivedEventCount() ) ;

		// echo $course_count;die();

	// -- cat -- //
		$event_cat_count = count( $innovare->getEventCatCount() ) ;

		// echo $course_count;die();
	

// -- GET SERVICES COUNT -- //

	// -- total -- //
		$total_service_count = count( $innovare->getServiceCount() ) ;

		// echo $course_count;die();

	// -- active -- //
		$active_service_count = count( $innovare->getActiveServiceCount() ) ;

		// echo $course_count;die();

	// -- archived -- //
		$archived_service_count = count( $innovare->getArchivedServiceCount() ) ;

		// echo $course_count;die();

	
	

?>